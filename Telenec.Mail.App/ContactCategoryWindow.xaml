<Window x:Class="Telenec.Mail.App.ContactCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telenec Mail — Neue Kategorie"
        Width="430"
        Height="205"
        MinWidth="380"
        MinHeight="190"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource Surface.Background}"
        Foreground="{StaticResource Text.Primary}"
        FontFamily="{StaticResource Typography.FontFamily}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Text="Neue Kategorie"
            FontSize="20"
            FontWeight="SemiBold"
            Foreground="{StaticResource Text.Primary}" />

        <TextBlock
            Grid.Row="1"
            Margin="0,5,0,14"
            Text="Die Kategorie steht anschließend auch für andere Kontakte zur Auswahl."
            FontSize="11"
            TextWrapping="Wrap"
            Foreground="{StaticResource Text.Secondary}" />

        <TextBox
            x:Name="CategoryNameTextBox"
            Grid.Row="2"
            Height="38"
            Padding="10,5"
            VerticalContentAlignment="Center" />

        <Grid
            Grid.Row="3"
            Margin="0,16,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="1"
                Width="100"
                Height="36"
                Content="Abbrechen"
                IsCancel="True"
                Cursor="Hand" />

            <Button
                Grid.Column="3"
                Width="110"
                Height="36"
                Content="Hinzufügen"
                IsDefault="True"
                Cursor="Hand"
                Style="{StaticResource Button.Primary}"
                Click="AddButton_OnClick" />

        </Grid>

    </Grid>

</Window>